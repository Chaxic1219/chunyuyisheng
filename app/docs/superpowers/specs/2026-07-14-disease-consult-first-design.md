# 问病优先引导问诊 + 无意义静默

**日期：** 2026-07-14  
**状态：** 已确认（主人选方案 3 + 无意义 A∪B）

## 目标

1. 低危自动回中，询问症状/疾病优先引导「101」问诊；常识靠后；缺「101」硬兜底。
2. 群内无实际表达意义的消息不触发 AI 回复（业务群也适用）。

## 特殊情况（不改）

高危 / 中危转人工；明确挂号、加号、住院、出诊等服务流程仍走对应编号。

## 无意义（A∪B）

- A：好的/收到/嗯/哈哈/谢谢/早晚安/+1/纯表情标点等
- B：紧凑后 ≤3 字且无症状/业务/菜单/编号意图

`@助手` 仍放行。业务群跳过旧闲聊门控时，仍要跑无意义静默。

## 实现要点

- `group_gate.js`：`isMeaninglessNoise`；扩症状词；门控末段静默
- `qiwe_bridge.js`：业务群路径补静默
- `triage.js`：低危提示词 + 问病缺 101 降级模板
- `server.js`：默认 `lowRiskReply` 文案对齐
