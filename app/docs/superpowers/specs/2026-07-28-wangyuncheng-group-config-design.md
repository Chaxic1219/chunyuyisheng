# 王云程医生群功能配置补全

**日期：** 2026-07-28  
**状态：** 待实施  
**范围：** `seed.js` 中王云程医生的 `content`、`rules`、`faq`、欢迎语与编号脚本补全  
**非范围：** 新页面开发、独立海报设计、医院挂号系统深度联调、春雨页面级新短链采集

---

## 1. 目标

基于用户提供的运营表，将王云程医生现有“基础信息已配置”的状态补全为一套可运行的群功能配置，优先保证：

1. 入群欢迎语可直接使用。  
2. `101/102/103/105/201/301/302/606/616/626/808/818/909/919/929` 可用。  
3. 不新增页面，优先复用现有患者端/春雨短链承接。  
4. 与其他医生隔离，不串用吕富靖/周玉春等医生专属内容。

---

## 2. 已确认决策

| 项 | 决策 |
|----|------|
| 模板来源 | 复用 **周玉春轻配置模板** 作为骨架 |
| 主页类入口 | 使用春雨小程序短链 `#小程序://春雨医生/T2ZrR81CrFmWgfG` |
| 主页类承接编号 | `101`、`102`、`301`、`808`、`818`、`909` |
| 不做项 | `202`、`501`、`888` 不接入菜单/规则 |
| 住院/评价表单 | `302`、`919` 使用用户提供的现有问卷链接 |
| 直接外链项 | `616`、`626` 直接使用医院页面链接 |

---

## 3. 配置映射

### 3.1 欢迎与群入口

- `groupWelcome` 改为用户提供的王云程版欢迎语。
- `memberVisit` 保持现有系统口径，仅将医生名/编号指向王云程配置。
- `menu` / `quickKeywords` 调整为王云程实际开放编号：
  - `101`
  - `102`
  - `103`
  - `105`
  - `201`
  - `301`
  - `302`
  - `606`
  - `616`
  - `626`
  - `808`
  - `818`
  - `909`
  - `919`
  - `929`

### 3.2 编号脚本

- `code101`、`code102`：使用用户提供引导语，卡片承接到王云程春雨主页短链。
- `code103`：直接返回医院电话 `010-67992043`。
- `code105`：沿用现有“查看回复”能力与文案。
- `code201`：使用用户提供挂号引导语；规则响应使用医院官方路径承接。
- `code301`：使用用户提供加号引导语；卡片承接到王云程春雨主页短链。
- `code302`：使用用户提供住院须知；规则响应跳问卷 `https://www.chunyuyisheng.com/rec/lsx2qjqsu6`。
- `code606`：使用用户提供科普渠道文案，不强依赖本地科普页。
- `code616`：不加额外提示语，直接弹医院知识链接。
- `code626`：不加额外提示语，直接弹医院 FAQ / 医保说明链接。
- `code808`：不加额外提示语，直接走王云程春雨主页短链。
- `code818`：使用用户提供转发引导语，先走同一主页短链承接。
- `code909`：使用用户提供感谢文案，先走王云程春雨主页短链承接。
- `code919`：使用用户提供引导语，跳问卷 `https://www.chunyuyisheng.com/rec/sewaya7aqe`。
- `code929`：使用用户提供感谢信引导语，走本地感谢信能力。

### 3.3 规则响应

规则层尽量复用现有系统能力，仅替换文案与目标链接：

- `101/102/301/808/818/909`：统一挂王云程主页短链。
- `302/919`：使用外部问卷链接卡。
- `616/626`：使用医院官方链接卡。
- `929`：使用本地感谢信入口。

---

## 4. 缺失素材处理

以下内容如果当前库里没有真实素材，不阻塞本次落地：

- 医生介绍页中的丰富图文素材
- 独立亲友海报图片
- 独立“送心意”页面级短链
- 医生风采/新闻报道/病例分享完整内容

本次优先目标是**把群服务编号跑通**；素材缺口先以主页入口或轻量文案承接，不伪造新内容。

---

## 5. 验证

实施后至少验证：

1. `/api/bootstrap?doctor=<王云程 slug>` 返回王云程菜单与快捷编号。  
2. `1` 菜单中只出现约定编号。  
3. `101/102/301/808/818/909` 都指向同一王云程主页短链。  
4. `302/919/616/626` 分别打开正确问卷/医院链接。  
5. 欢迎语与 `code103/code201/code606/code929` 文案为用户提供版本。  

