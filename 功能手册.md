# 春雨医生 · 患者服务中心 — 功能手册

> 版本：2.0.0 | 部署版本：20260707-160038-a817917  
> 服务器：115.190.210.129 | 路径：/opt/chunyu-doctor/releases/20260707-160038-a817917/app  
> 技术栈：纯 Node.js（≥22），零 npm 运行时依赖，内置 node:sqlite

---

## 一、系统角色

| 角色 | 说明 | 权限 |
|------|------|------|
| **患者** | 通过 H5 患者端 / 企微群 / 企微单聊 接入 | 浏览医生信息、AI 咨询、提交表单（联络表/加号/住院/感谢信）、查看随访、写口碑评价 |
| **医助/运营** | 后台 `/admin` 登录 | 管理提交记录、分诊审核(approve/reject AI 草稿)、社群消息审核与发送、随访队列管理、FAQ/规则/医生管理 |
| **超级管理员（super）** | admins.role = 'super' | 全部医生数据、企微/QiWe 凭证配置、子管理员管理 |
| **子管理员（scoped）** | admins.role = 'scoped' | 仅可操作 admin_doctors 关联表分配的医生数据 |
| **医生** | 不直接登录系统（通过医助代操作） | 被展示方（名片/科普/出诊），医助把需医生定夺的分诊消息标为「转医生」 |
| **AI（MiMo/DeepSeek）** | 可插拔 LLM 接入 | 仅做草稿生成 + 低风险自动回复；high/medium 绝不自动发，需人工确认 |

---

## 二、功能模块总览

### 2.1 患者端（H5 / 企微内嵌）

| 功能 | 入口 | 说明 |
|------|------|------|
| 医生名片首页 | `/` | 头像 + 职称 + 认证徽标 + 三组功能卡片 |
| AI 在线咨询 | 对话框 | 功能码走关键词引擎快速返回卡片；自然语言走 AI 分诊 |
| 紧急度分级卡 | 咨询回复内 | emergency/urgent/soon/routine 四档 + 就诊时间窗 + 行动入口 |
| 医患联络表 | 功能卡 | 手机短信验证 + 敏感信息单独同意，写入数据库 |
| 门诊加号 | 414 / 功能卡 | 停诊时段服务端拦截 + 候补名单 + 家庭代办 + 必须先建档 |
| 住院预约 | 功能卡 | 家庭代办 + 即时同意 |
| 门诊时间 / 医院电话 | 303 / 114 | 结构化信息展示 |
| 医生风采 / 科普 / 视频 | 808 / 606 | 三段式 Tab + 外链 |
| 感谢医生（写感谢信） | 929 | 手机验证后提交，进后台待审 |
| 认证口碑墙 | 919 | 仅建档患者 + 本人短信验证可写，署名脱敏 |
| 我的随访 | 功能卡 | 术式/病种 SOP 时间轴 + 宣教/复诊提醒 |
| 我的提交/回复状态 | 功能卡 | 手机验证后查看本人历史提交 |
| 长辈模式 | 右上角 | 全站放大字体/按钮/高对比度 + 听一听 TTS |
| 多医生切换 | `?doctor=slug` / 顶栏下拉 | 前端路由 + API bootstrap |
| 春雨跳转 | 外链卡 | 短链 → 微信小程序 / H5 / URL Scheme |
| 按病种服务 | 功能卡 | 将相关功能打包为一站式服务包 |

### 2.2 医助后台（`/admin`）

| 模块 | 说明 |
|------|------|
| 提交记录 / 患者档案 | 查看/更新联络表/加号/住院/感谢信/口碑，修改状态 |
| AI 分诊台 | 分诊会话列表、风险等级/紧急度/六要素病历卡、草稿确认/编辑/拒绝 |
| 关键词规则管理 | CRUD 功能码 → 回复卡片映射，支持精确/包含匹配 |
| FAQ 管理 | 分组的常见问题 CRUD |
| 医生管理 | 新增/切换/激活/一键克隆（底座 + 规则 + FAQ） |
| 随访队列 | 入组/时间轴/节点状态流转/手动标记 |
| 候补名单 | 查看/释放/单独通知 |
| 社群运营 | 群配置/入站消息/出站队列审核/确认发送/取消/忽略/转医生/编辑 |
| 周运营科普 | 一键生成周五科普草稿（进待审队列） |
| 统计仪表盘 | 提交分类计数 + 消息量 + 分诊会话 + 社群待审 |
| 子管理员管理 | 创建 scoped 用户 + 分配医生 |
| 企微凭证配置 | WeChat/QiWe 凭证的安全保存（脱敏回显） |
| QiWe 小程序卡片 | 查看采集的 weapp 模板 + 预览回复 |
| 主动提醒（只读） | 待跟进的加号/住院预约计数与最近列表 |

### 2.3 AI 分诊引擎（`triage.js`）

| 层 | 职责 |
|----|------|
| 本地红旗规则（floor） | 确定性关键词扫描 → 高风险直接转人工（emergency/urgent），不调 LLM |
| 症状哨兵（sentinel） | 症状大类宽词 → L2 失灵时 fail-safe 升 medium |
| 人工触发（HUMAN_TRIGGERS） | 诊断/用药/手术决策意图 → medium |
| L2 风险天网（可选 LLM） | 仅上抬风险，不能降级（只升不降钳制） |
| 紧急度分级 | emergency → 120；urgent → 当天；soon → 3天；routine → 居家观察 |
| 自动发送规则 | low 可自动发（经词表+L2双重闸控）；medium 不自动发转人工；high 恒安全模板 |
| 图片/报告处理 | MiMo v2.5 多模态预审 → 中风险 → 转人工 |
| PII 脱敏 | LLM 提示词注入前 + 输出返回前双向脱敏 |

### 2.4 社群运营（`community.js`）

| 功能 | 说明 |
|------|------|
| 群配置管理 | 渠道类型/欢迎语/审核模式/外部群ID |
| 入站消息处理 | 编号 → 关键词引擎；自然语言 → AI 分诊；产物入出站队列(pending) |
| 出站队列 | 所有回复一律 pending → 医助确认发送/取消/编辑/忽略/转医生 |
| 群边界扫描 | 无关话题(spam/广告)、对医生不利言论 → 标记提醒医助（不自动踢） |
| 建群命名建议 | 按模板生成建议群名 |
| 周五定时科普 | 自动生成草稿进待审队列（绝不自动发） |
| 新患者入群提醒 | 首次发言检测 → 欢迎消息入队 |

### 2.5 企微/QiWe 接入

| 通道 | 说明 |
|------|------|
| QiWe 第三方（主通道） | `manager.qiweapi.com` 外挂，`/api/qiwe/callback` 回调，甲方裁定采用 |
| 官方企微自建应用（备用） | `wecom.js`，`/api/wecom/callback`，V1 未启用 |
| 企微侧边栏 | `/wecom-sidebar.html`，群内只读上下文 |
| 安全设计 | 回调鉴权 fail-closed + URL 令牌 + 防重放 + 大整数 ID 保真 |
| 出站 | 文本 + 小程序卡片（真机采集封面），经确认后真发 |

### 2.6 随访自动化（`followup.js`）

- 术式/病种 SOP 定义（医生 content.followupPlans）
- 建档选方案 → 自动入组
- 时间轴计算（术后 1/3/7/30 天节点）
- 节点状态流转：pending → pushed → done
- 医助队列 + 患者自查

### 2.7 患者身份枢纽（`db.js resolvePatient`）

- 跨渠道(短信/企微/QiWe/web)统一 patient_id
- 手机号验证标记
- 建档即收敛身份

---

## 三、业务流程

### 3.1 患者在线咨询完整流程

```
患者输入文本/图片
    │
    ├─ 功能码（101/303/414...）→ engine.match → 预置卡片回复
    │
    ├─ 菜单指令（3/菜单/功能）→ 返回动态菜单
    │
    └─ 自然语言/图片 → AI 分诊链路
         │
         ├─ 本地红旗扫描(scanRisk)
         │    ├─ high(emergency) → 安全模板 + "拨120" → 转人工
         │    ├─ high(urgent) → 安全模板 + "当天就诊" → 转人工
         │    ├─ medium → 不自动发 → 转人工
         │    └─ low → 可自动发
         │
         ├─ L2 风险天网(可选 LLM) → 只能上抬、不能降级
         │
         ├─ 图片 → MiMo 多模态 → 默认 medium → 转人工
         │
         └─ low 自动回复路径
              ├─ 词表扫描(postScanLowRiskReply)
              ├─ L2 语义复检(recheckReplyLLM)
              ├─ 双闸都过 → maskPIIStrict → 发送
              └─ 任一闸不过 → 降级安全模板
```

### 3.2 门诊加号流程

```
患者发送 404 / 点击加号入口
    │
    ├─ 检查是否已建档（联络表）→ 未建档 → 引导先提交联络表
    │
    ├─ 选择就诊时段
    │    ├─ 不在白名单 → 400 拒绝
    │    ├─ 在停诊黑名单 → 409 + 候补入口
    │    └─ 有效时段 → 继续
    │
    ├─ 即时同意（PIPL）
    │
    └─ 提交 → 入库 → 医助跟进
```

### 3.3 企微群消息处理流程

```
企微群内患者发消息
    │
    ├─ QiWe 回调 → 鉴权(fail-closed) → 大整数 ID 保真解析
    │
    ├─ 去重(qiwe_seen) → 白名单(testToId 范围)
    │
    ├─ 群门控(group_gate)
    │    ├─ @bot → 响应
    │    ├─ 编号精确命中 → 响应
    │    ├─ 高风险 → 响应
    │    └─ 普通闲聊 → 静默不处理
    │
    ├─ 新成员首现 → 欢迎消息入队(pending)
    │
    ├─ 命中规则/分诊 → 产出入出站队列(pending)
    │
    └─ 医助在后台确认发送 → QiWe API 真发（文本/小程序卡片）
```

### 3.4 随访 SOP 流程

```
患者提交联络表 → 选择随访方案
    │
    └─ 自动入组 → 生成时间轴节点
         │
         ├─ 术后第 1 天 → 宣教推送
         ├─ 术后第 3 天 → 用药提醒
         ├─ 术后第 7 天 → 复查提醒
         └─ 术后第 30 天 → 门诊复诊
              │
              └─ 医助在后台推送/标记完成
```

### 3.5 口碑评价流程

```
患者点击「评价医生」(919)
    │
    ├─ 检查是否已建档（联络表） → 未建档 → 拒绝
    │
    ├─ 手机短信验证（本人验证）
    │
    ├─ 评价内容 ≥ 10 字
    │
    ├─ 署名 = 建档姓名脱敏（不接受自填）
    │
    └─ PII 脱敏 → 入库(status='已认证') → 口碑墙公开展示
```

---

## 四、数据库（SQLite，30 张表）

| 表 | 用途 |
|----|------|
| doctors | 医生档案（slug/名片/内容JSON/出诊/规则配置） |
| rules | 功能码 → 回复卡片映射 |
| faq | 常见问题分组 |
| submissions | 患者所有表单提交（联络表/加号/住院/感谢/口碑） |
| admins | 管理员（username + scrypt hash） |
| admin_doctors | 子管理员 ↔ 医生多对多关联 |
| msg_log | 消息日志 |
| triage_sessions | 分诊会话 |
| triage_messages | 分诊消息（含 AI 草稿/最终文本/附件） |
| triage_decisions | 分诊决策记录（风险等级/紧急度/六要素/模型） |
| followups | 随访记录（方案/节点JSON/状态） |
| waitlist | 候补名单 |
| community_groups | 社群群配置 |
| community_members | 群成员 |
| community_messages | 社群入站消息 |
| outbound_queue | 出站消息队列（pending→sent/cancelled/ignored） |
| patients | 患者主表（统一身份枢纽） |
| patient_identities | 跨渠道身份关联 |
| hospitals | 院级信息 |
| wecom_configs | 企微凭证 |
| wecom_identities | 企微用户身份 |
| qiwe_configs | QiWe 第三方凭证 |
| qiwe_weapp_templates | 小程序卡片模板（封面/路径/appId） |
| qiwe_seen | 回调去重 |
| qiwe_room_member_seen | 群成员首现登记 |
| ops_strategy | 运营策略 |
| ops_configs | 运营配置（话术/安全规则等，domain 分域） |
| ops_config_audit | 配置变更审计 |
| knowledge_items | 知识库条目 |
| knowledge_vectors | RAG 向量存储 |
| outcome_reports | 效果评估报告 |
| schema_patches | 迁移记录（一次性 patch 幂等控制） |

---

## 五、安全设计要点

| 维度 | 实现 |
|------|------|
| AI 安全 | 高风险绝不调 LLM、绝不自动发；风险只升不降（钳制层）；fail-closed |
| PII | 存储侧白名单掩码 + LLM 提示词脱敏 + 输出脱敏 + 公开展示脱敏 |
| 认证 | 短信验证码 + cookie session + scrypt 密码 + 短信节流 + 尝试次数限制 |
| 回调鉴权 | fail-closed（未配 secret 一律拒绝） + URL 令牌通道 + 防重放 |
| 数据库 | WAL + foreign_keys + busy_timeout + 迁移补列幂等 |
| 企微出站 | 所有内容入队 pending → 人工确认 → 才真发（零自动发不变量） |
| PIPL 合规 | 敏感信息单独同意 + 即时同意 + 最小字段收集 |
| 注入防护 | 参数化 SQL + body 大小限制 + 路径遍历防护 + 大整数 JSON 保真 |

---

## 六、部署与运维

```bash
# 启动（生产）
ADMIN_PASSWORD=<强密码> node server.js

# 启动（演示/测试）
node server.js --demo

# 测试
node _unittest.js   # 646 离线单元测试
node _fulltest.js   # 全接口测试（需 --demo 服务运行）
node _uitest.js     # 前端端到端（需 jsdom）
```

| 环境变量 | 用途 |
|----------|------|
| ADMIN_PASSWORD | 后台管理员密码 |
| MIMO_API_KEY | MiMo AI（优先） |
| DEEPSEEK_API_KEY | DeepSeek AI（回退） |
| TRIAGE_AI_DISABLED=1 | 强制只走本地模板 |
| LOW_RISK_LLM_REPLY=1 | 低风险启用 LLM 自动回复 |
| QIWE_TOKEN / QIWE_GUID | QiWe 第三方凭证 |
| QIWE_CALLBACK_SECRET | QiWe 回调密钥 |
| WECOM_CORP_ID / SECRET | 企微官方凭证（备用） |
| COMMUNITY_WEBHOOK_TOKEN | 社群入站 token |
| WECHAT_OA_APP_ID / SECRET | 公众号 JS-SDK 签名 |
| PUBLIC_ORIGIN | 深链绝对路径前缀 |
| WEEKLY_OPS_AUTO=1 | 周五科普自动草稿 |

---

## 七、关键编号（功能码）

| 编号 | 功能 | 交付方式 |
|------|------|----------|
| 3 | 全部功能菜单 | 动态文本 |
| 101 | 医生咨询 | 文字 + 二维码/小程序卡片 |
| 102 | 视频问诊 | 春雨小程序跳转 |
| 114 | 医院电话 | 弹窗 |
| 202 | 查看回复/我的订单 | 春雨短链 |
| 303 | 挂号及门诊时间 | 外链 + 说明 |
| 404 | 门诊加号 | 本地表单 + 停诊拦截 + 候补 |
| 414 | 住院预约 | 本地表单/小程序问卷 |
| 606 | 科普合集 | 4张真实科普外链 |
| 616 | 住院及手术知识 | 公众号外链 |
| 626 | 就医常见问题 | 公众号外链 |
| 808 | 医生简介展示 | 春雨 h5_webview 小程序卡片 |
| 818 | 介绍给亲友 | 海报图片 |
| 909 | 感谢医生(送心意) | 小程序卡片(send_heart) |
| 919 | 评价医生 | 小程序问卷 + 口碑墙 |
| 929 | 写感谢信 | 本地表单 |

---

## 八、文件结构

```
app/
├── server.js          主服务（HTTP + 路由 81 条 + 鉴权 + 安全）
├── db.js              SQLite 建表 30 张 + 种子 + 迁移 + 身份枢纽
├── engine.js          关键词规则引擎（精确/包含匹配 + 风险闸控）
├── triage.js          AI 分诊（红旗/哨兵/L2天网/紧急度/自动发规则）
├── patient_reply.js   患者消息统一回复链路
├── followup.js        随访自动化
├── community.js       社群运营（群配置/入站/出站/边界扫描/科普）
├── group_gate.js      群消息接管门控（静默/响应判定）
├── wecom.js           企微官方接入（验签/加解密/发送）
├── qiwe.js            QiWe 第三方接入（配置/事件归一化/API调用）
├── qiwe_bridge.js     QiWe 单聊桥（回调→回复→真发）
├── authz.js           鉴权判定纯函数（fail-closed）
├── pii.js             PII 掩码单一模块
├── ops_config.js      运营配置运行时读取
├── seed.js            种子数据（3位医生：吕富靖/黄安华/郭强）
├── public/            前端静态文件
│   ├── index.html     患者端 SPA
│   ├── admin.html     医助后台 SPA
│   ├── app.css        设计系统
│   ├── wecom-sidebar.html  企微侧边栏
│   └── src/           前端 JS
└── _*.js              测试/截图/探测脚本
```
